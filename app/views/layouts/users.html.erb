<!DOCTYPE html>
<html>
  <head>
    <title>ら～めんクエスト</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= favicon_link_tag('favicon.ico') %>
    
    <%= stylesheet_link_tag    'users', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>
  <body>
    <div class="wrapper">
      <header>
        <%= render partial: 'users/shared/header' %>
        <%= render partial: 'users/shared/breadcrumb' %>
      </header>
      <main>
        <div class="container">
          <% flash.each do |message_type, message| %>
            <div class="alert alert-<%= message_type %> m-2"><%= message %></div>
          <% end %>
          <%= yield %>
        </div>
      </main>
      <footer>
        <%= render partial: 'users/shared/footer' %>
      </footer>
    </div>
  </body>
</html>