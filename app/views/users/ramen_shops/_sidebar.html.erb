<aside>
  <div class="card bg-white border-none shadow-sm mb-3">
    <div class="btn-group">
      <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        都道府県別で絞り込む <span class="caret"></span>
      </button>
      <ul class="dropdown-menu scrollable-menu">
        <% (1..47).each do |n|   %>
          <%= link_to ramen_shops_path(prefecture: n) do %>
            <li class="dropdown-item">
              <%= RamenShop.prefectures.key(n) %>
            </li>
          <% end %>
        <% end %>
      </ul>
    </div>
  </div>
  <div class="card bg-white border-none shadow-sm">
    <div class="card-body p-3">
      <%= link_to new_ramen_shop_path, class: "btn-block btn btn-success" do %>
        <i class="fas fa-edit"></i> 店舗情報を追加する
      <% end %>
      <small>
        <p class="pt-3">お探しの店舗が無かった場合はこちらから追加↑↑</p>
      </small>
    </div>
  </div>
</aside>